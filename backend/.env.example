# Local Docker Postgres:
DATABASE_URL="postgresql://aws_trainer:aws_trainer@localhost:5433/aws_trainer?schema=public"

# Supabase remote Postgres via session pooler:
# DATABASE_URL="postgresql://postgres.<project-ref>:<password>@<pooler-host>:5432/postgres?schema=public&sslmode=require&uselibpqcompat=true"

SESSION_TTL_DAYS="7"
